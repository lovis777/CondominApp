<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.condominio.proy.condominapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".InfoPersonalActivity" />
        <activity android:name=".AmbCompartidoActivity" />
        <activity android:name=".ObligacionesActivity" />
        <activity android:name=".AC_PiscinaActivity" />
        <activity android:name=".LuzActivity" />
        <activity android:name=".AguaActivity" />
        <activity android:name=".MantenimientoActivity" />
        <activity android:name=".CuotaDepaActivity" />
        <activity android:name=".PenalidadesActivity" />
    </application>

</manifest>